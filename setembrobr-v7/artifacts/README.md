# Durable artifacts

This directory keeps aggregate, non-user-level reports and hash-only provenance copied
back from the completed Fedora run. Restricted PKLs, embeddings, user-level scores,
manifests, fitted models, and temporary Parquet files remain under `.work/` or
`artifacts/restricted/` and are ignored by Git.
